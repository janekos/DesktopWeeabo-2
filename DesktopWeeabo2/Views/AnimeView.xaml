<UserControl x:Class="DesktopWeeabo2.Views.AnimeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DesktopWeeabo2.Views"
             xmlns:enum="clr-namespace:DesktopWeeabo2.ViewModels.Shared"
             xmlns:conv="clr-namespace:DesktopWeeabo2.Helpers"
             x:Name="AnimeViewControl"
             mc:Ignorable="d"
             d:DesignHeight="730" d:DesignWidth="1004">
    <UserControl.Resources>
        <conv:ViewStatusToVisibilityConverter x:Key="ViewStatusToVisibilityConverter" />
        <conv:SelectedItemToVisibilityConverter x:Key="SelectedItemToVisibilityConverter" />
        <conv:StringLengthToVisibilityConverter x:Key="StringLengthToVisibilityConverter" />
        <Style x:Key="InfoBoxTextBlock" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>
    </UserControl.Resources>
    <Grid Background="White">
        <TextBlock Foreground="Black" FontSize="30" HorizontalAlignment="Left" VerticalAlignment="Top">Anime items</TextBlock>
        <Button Content="Online" HorizontalAlignment="Left" Margin="171,10,0,0" VerticalAlignment="Top" Width="60" Height="28" Command="{Binding ChangeItemSource}" CommandParameter="{x:Static enum:StatusView.Online}"/>
        <Button Content="To watch" HorizontalAlignment="Left" Margin="231,10,0,0" VerticalAlignment="Top" Width="60" Height="28" Command="{Binding ChangeItemSource}" CommandParameter="{x:Static enum:StatusView.Towatch}"/>
        <Button Content="Viewed" HorizontalAlignment="Left" Margin="291,10,0,0" VerticalAlignment="Top" Width="60" Height="28" Command="{Binding ChangeItemSource}" CommandParameter="{x:Static enum:StatusView.Viewed}"/>
        <Button Content="Watching" HorizontalAlignment="Left" Margin="351,10,0,0" VerticalAlignment="Top" Width="60" Height="28" Command="{Binding ChangeItemSource}" CommandParameter="{x:Static enum:StatusView.Watching}"/>
        <Button Content="Dropped" HorizontalAlignment="Left" Margin="411,9,0,0" VerticalAlignment="Top" Width="60" Height="28" Command="{Binding ChangeItemSource}" CommandParameter="{x:Static enum:StatusView.Dropped}"/>
        <TextBox HorizontalAlignment="Left" Height="28" Margin="476,9,0,0" TextWrapping="Wrap" Text="{Binding Path=SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, Delay=500}" VerticalAlignment="Top" Width="235" FontSize="18"/>
        <TextBlock HorizontalAlignment="Right" Margin="0,10,0,0" TextWrapping="Wrap" Text="{Binding Path=TotalItems, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Top" MinWidth="10" FontSize="20"/>
        <Grid Margin="0,46,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition MinWidth="330" Width="4*"/>
                <ColumnDefinition Width="10" />
                <ColumnDefinition MinWidth="330" Width="2*" />
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" BorderBrush="Black" BorderThickness="1">
                <ListView SelectionMode="Single" ItemsSource="{Binding AnimeItems}" SelectedItem="{Binding Path=SelectedItem, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="Black" BorderThickness="1" Margin="5" Padding="3" Width="220">
                                <StackPanel Orientation="Vertical" >
                                    <Image Source="{Binding CoverImage}" Margin="-3,-3,-3,3"/>
                                    <TextBlock Text="{Binding TitleEnglish}" TextWrapping="Wrap" Visibility="{Binding Path=TitleEnglish, Converter={StaticResource StringLengthToVisibilityConverter}}"></TextBlock>
                                    <TextBlock Text="{Binding TitleRomaji}" TextWrapping="Wrap" Visibility="{Binding Path=TitleRomaji, Converter={StaticResource StringLengthToVisibilityConverter}}"></TextBlock>
                                    <TextBlock Text="{Binding TitleNative}" TextWrapping="Wrap" Visibility="{Binding Path=TitleNative, Converter={StaticResource StringLengthToVisibilityConverter}}"></TextBlock>
                                    <TextBlock Text="{Binding ViewingStatus}" TextWrapping="Wrap" Visibility="{Binding Path=ViewingStatus, Converter={StaticResource StringLengthToVisibilityConverter}}"></TextBlock>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                    <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>
                </ListView>
            </Border>
            <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" />
            <Border Grid.Column="2" BorderBrush="Black" BorderThickness="1">
                <Grid>
                    <Grid Height="30" VerticalAlignment="Top">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="1*"/>
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="0" Content="To watch" BorderThickness="1,1,1,2" Command="{Binding SaveAnimeItemToDb}" CommandParameter="{x:Static enum:StatusView.Towatch}"/>
                        <Button Grid.Column="1" Content="Viewed" BorderThickness="1,1,1,2" Command="{Binding SaveAnimeItemToDb}" CommandParameter="{x:Static enum:StatusView.Viewed}"/>
                        <Button Grid.Column="2" Content="Watching" BorderThickness="1,1,1,2" Command="{Binding SaveAnimeItemToDb}" CommandParameter="{x:Static enum:StatusView.Watching}"/>
                        <Button Grid.Column="3" Content="Dropped" BorderThickness="1,1,1,2" Command="{Binding SaveAnimeItemToDb}" CommandParameter="{x:Static enum:StatusView.Dropped}"/>
                    </Grid>
                    <ScrollViewer VerticalScrollBarVisibility="Auto" VerticalAlignment="Bottom" Margin="0,30,0,0">
                    <StackPanel DataContext="{Binding Path=SelectedItem, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                        <Image Source="{Binding CoverImage}" Margin="-3,-3,-3,3" Height="450"/>
                        <TextBlock Style="{StaticResource InfoBoxTextBlock}" Visibility="{Binding Path=TitleEnglish, Converter={StaticResource StringLengthToVisibilityConverter}}">
                            <Run Text="English: " FontWeight="Bold"></Run>
                            <Run Text="{Binding TitleEnglish}"></Run>
                        </TextBlock>
                        <TextBlock Style="{StaticResource InfoBoxTextBlock}" Visibility="{Binding Path=TitleRomaji, Converter={StaticResource StringLengthToVisibilityConverter}}">
                            <Run Text="Romaji: " FontWeight="Bold"></Run>
                            <Run Text="{Binding TitleRomaji}"></Run>
                        </TextBlock>
                        <TextBlock Style="{StaticResource InfoBoxTextBlock}" Visibility="{Binding Path=TitleNative, Converter={StaticResource StringLengthToVisibilityConverter}}">
                            <Run Text="Native: " FontWeight="Bold"></Run>
                            <Run Text="{Binding TitleNative}"></Run>
                        </TextBlock>
                        <TextBlock Style="{StaticResource InfoBoxTextBlock}" Visibility="{Binding Path=MeanScore, Converter={StaticResource StringLengthToVisibilityConverter}}">
                            <Run Text="Mean score: " FontWeight="Bold"></Run>
                            <Run Text="{Binding MeanScore}"></Run>
                        </TextBlock>
                        <TextBlock Style="{StaticResource InfoBoxTextBlock}" Visibility="{Binding Path=Format, Converter={StaticResource StringLengthToVisibilityConverter}}">
                            <Run Text="Format: " FontWeight="Bold"></Run>
                            <Run Text="{Binding Format}"></Run>
                        </TextBlock>
                        <TextBlock Style="{StaticResource InfoBoxTextBlock}" Visibility="{Binding Path=Status, Converter={StaticResource StringLengthToVisibilityConverter}}">
                            <Run Text="Status: " FontWeight="Bold"></Run>
                            <Run Text="{Binding Status}"></Run>
                        </TextBlock>
                        <TextBlock Style="{StaticResource InfoBoxTextBlock}" Visibility="{Binding Path=Description, Converter={StaticResource StringLengthToVisibilityConverter}}">
                            <Run Text="Description: " FontWeight="Bold"></Run>
                            <Run Text="{Binding Description}"></Run>
                        </TextBlock>
                        <TextBlock Style="{StaticResource InfoBoxTextBlock}" Visibility="{Binding Path=Genres, Converter={StaticResource StringLengthToVisibilityConverter}}">
                            <Run Text="Genres: " FontWeight="Bold"></Run>
                            <Run Text="{Binding Genres}"></Run>
                        </TextBlock>
                        <TextBlock Style="{StaticResource InfoBoxTextBlock}" Visibility="{Binding Path=Synonyms, Converter={StaticResource StringLengthToVisibilityConverter}}">
                            <Run Text="Synonyms: " FontWeight="Bold"></Run>
                            <Run Text="{Binding Synonyms}"></Run>
                        </TextBlock>
                        <TextBlock Style="{StaticResource InfoBoxTextBlock}" Visibility="{Binding Path=Episodes, Converter={StaticResource StringLengthToVisibilityConverter}}">
                            <Run Text="Number of episodes: " FontWeight="Bold"></Run>
                            <Run Text="{Binding Episodes}"></Run>
                        </TextBlock>
                        <TextBlock Style="{StaticResource InfoBoxTextBlock}" Visibility="{Binding Path=Duration, Converter={StaticResource StringLengthToVisibilityConverter}}">
                            <Run Text="Episode duration: " FontWeight="Bold"></Run>
                            <Run Text="{Binding Duration}"></Run>
                        </TextBlock>
                        <TextBlock Style="{StaticResource InfoBoxTextBlock}" Visibility="{Binding Path=StartDate, Converter={StaticResource StringLengthToVisibilityConverter}}">
                            <Run Text="Start date: " FontWeight="Bold"></Run>
                            <Run Text="{Binding StartDate}"></Run>
                        </TextBlock>
                        <TextBlock Style="{StaticResource InfoBoxTextBlock}" Visibility="{Binding Path=EndDate, Converter={StaticResource StringLengthToVisibilityConverter}}">
                            <Run Text="End date: " FontWeight="Bold"></Run>
                            <Run Text="{Binding EndDate}"></Run>
                        </TextBlock>
                    </StackPanel>
                </ScrollViewer>
                </Grid>
            </Border>
            <Border Grid.Column="2" VerticalAlignment="Stretch" Background="White" BorderBrush="Black" BorderThickness="1" Visibility="{Binding Path=SelectedItem, Converter={StaticResource SelectedItemToVisibilityConverter}}">
                <Label VerticalContentAlignment="Center" HorizontalContentAlignment="Center">
                    <TextBlock FontSize="40" TextWrapping="Wrap" TextAlignment="Center">Try clicking on an item</TextBlock>
                </Label>
            </Border>
        </Grid>
    </Grid>
</UserControl>
